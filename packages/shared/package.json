{
  "name": "@readease/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript contracts, models, and utilities for ReadEase",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo \"No tests for shared package yet\""
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
